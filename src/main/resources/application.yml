spring:
  data:
    mongodb:
      host: localhost
      database: commit-viwer-mongodb

---
spring:
  profiles:
    active: docker
  data:
    mongodb:
      host: mongodb
      database: commit-viwer-mongodb